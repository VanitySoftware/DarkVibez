"Oceanized" by ProjectShadow (v1.0)
For DarkVibez v1.5 and higher (can change anytime)


HOW TO APPLY:
1. Open darkvibez.theme.css from your themes folder
2. Replace your existing Config with this one


/* Config ("Oceanized v1.0" by ProjectShadow) */
    --background-1: #001525; /* Background used for sidebar-like things*/
    --background-2: linear-gradient(to top right, #001141, #0d2872); /* Background used for chats, friend list etc. */
    --background-3: #092f68; /* Background used for things like the search bar and the second area of a vcs usercount */
    --bg-usercount: #294155; /* Background used for the first part of a vcs usercount */
    --bg-typebox: #0f2055; /* Background used for the type box below the chat */
    --bg-card: #00318b; /* Background used for the cards in the Active now area */
    --bg-card-2: #00215e; /* Background used for the inner cards in the Active now area */
    --topleft-border-radius: 12px !important; /* border-radius, used in the top-left of every page | HAS to be set to !important*/
    --attachment-bg-file: linear-gradient(90deg, #0047b3 0%, #1b1b1b 55%, #1b1b1b 100%); /* The background of a file attachment */
    --attachment-bg-code: #000000; /* The background of a code/text attachment */
    --attachment-radius: 12px; /* Border-Radius for the attachments */
    --color-scheme: #0e3da1; /* Color scheme used for some buttons and other things*/
    --font-primary: 'Heebo', sans-serif !important; /* Replace Font | HAS to be set to !important*/
    --btn-hovered: #131a58; /* Background used for most buttons in hover mode */
    --btn-selected: #0d1779; /* Background used for most buttons in selected mode */